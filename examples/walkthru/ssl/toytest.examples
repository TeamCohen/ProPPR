predict(pos,X)	+predict(pos,pricybarbie)	+predict(pos,yellowcar)	+predict(pos,redbike2)	+predict(pos,redporshe)	-predict(pos,bigpile)	-predict(pos,hugeEmail)	-predict(pos,woetrouble)
predict(neg,X)	-predict(neg,pricybarbie)	-predict(neg,yellowcar)	-predict(neg,redbike2)	-predict(neg,redporshe)	+predict(neg,bigpile)	+predict(neg,hugeEmail)	+predict(neg,woetrouble)
